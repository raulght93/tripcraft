{
  "name": "tripcraft",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Herramienta open-source de planificación de viajes (motor agnóstico al viaje).",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "trips/*",
    "apps/*",
    "worker"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --test \"packages/**/test/*.test.ts\"",
    "typecheck": "tsc -b packages/schema packages/engine"
  },
  "engines": {
    "node": ">=22.6"
  }
}
