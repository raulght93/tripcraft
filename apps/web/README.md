# @tripcraft/web — STUB

SPA Vite + React (framework-agnóstico, no Next.js — ver
[`docs/hosting-comparison.md`](../../docs/hosting-comparison.md)).

**Pendiente (Fase 1):** portar el orquestador de `africa-trip-planning`
(`AfricaGuide.jsx`, componentes presentacionales, hooks) a un shell genérico que
consuma un documento `Trip` vía `@tripcraft/engine`, con las tabs/forks derivadas
de `trip.sequence` en vez de cableadas.
